# CCE — Capital Control Engine
# Install:  python -m venv .venv
#           .venv/Scripts/python.exe -m pip install -r requirements.txt   (Windows)
#           .venv/bin/python -m pip install -r requirements.txt           (Unix)

# ── Market data ──────────────────────────────────────────────
# Installed from GitHub (project decision), not PyPI.
#
# Pinned to a commit, not a branch: `git+https://…` without @<sha>
# resolves to whatever HEAD is at install time, which is not a pin
# and would break reproducibility (NFR-012).
# This commit resolves to jugaad-data 0.35.5.
#
# NOTE: jugaad-data does NOT declare pandas as a dependency — its
# *_df DataFrame API imports it lazily, so pandas must be explicit.
jugaad-data @ git+https://github.com/jugaad-py/jugaad-data.git@7ae415e5ed5cbbe805bcc43d8bbb4b4ab97c6000

# ── Numerics ─────────────────────────────────────────────────
numpy==2.4.6
pandas==3.0.5

# ── Phase 0: config, contracts, tests ────────────────────────
PyYAML==6.0.3
python-dotenv==1.2.3
pytest==9.1.1

# ── Phase 4: optimizer (installed early, deliberately) ───────
# cvxpy pulls compiled solvers and is the one dependency likely to fail
# on Windows. Installed and smoke-tested during phase 0 rather than at
# its own phase: discovering a broken solver at hour 8 costs far more
# than discovering it at hour 1. Verified solving to status "optimal".
cvxpy==1.9.2

# ── Not yet installed — uncomment as each build phase starts ──
# Phases per docs/IMPLEMENTATION-PLAN.md §0 "Dependency timeline".
# Pin each to the version you actually install and verify.
pyarrow==25.0.1        # phase 1 — parquet cache
scipy==1.17.1          # phase 3 — risk engine, HRP clustering
streamlit==1.63.0      # phase 10 — dashboard
plotly==7.0.0          # phase 10 — charts
anthropic==1.4.0       # phase 13 — OPTIONAL; the system runs fully without a key

# ── Quality gate ─────────────────────────────────────────────
# Pinned deliberately. Before pyproject.toml existed these were unpinned and
# unconfigured, so "the lint is clean" meant whatever the installed ruff
# happened to default to that week — a quality gate that moves on its own is
# not a gate (NFR-012). The rule set lives in pyproject.toml.
ruff==0.16.6
mypy==2.3.1
